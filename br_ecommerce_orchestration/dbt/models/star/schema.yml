
version: 2

models:
  - name: DIM_CUSTOMERS
    description: "A starter dbt model"
    columns:
      - name: customer_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
